# Sport Team Manager mobile

Flutter bootstrap with Firebase authentication, a verified API session, a feature-first layout and a centralized, club-aware Material 3 design system.

The bootstrap deliberately uses only Flutter SDK navigation and state primitives. The lightweight `http` package is the validated networking choice for the MVP foundation; state management, routing and model/code-generation packages remain undecided.
